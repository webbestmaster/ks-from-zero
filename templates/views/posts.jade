extends ../layouts/base

block content
    h1 Hello World!
    - for (var i=0; i < posts.length; i++) {
        - var post = posts[i];
        p= post.title
            a(href= '/posts/' + post.slug )= post.title
    - }

